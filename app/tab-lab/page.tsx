
'use client'
import React from 'react'
import { supabase } from '../../lib/supabaseClient'
export default function TabLab(){return (<div style={{padding:16}}>
  <h1>Lab – Import “Système / Sous-système / Dénomination / Repère”</h1>
  <p>Remplacez ce fichier par la version complète du TabLab si besoin (celle déjà fournie). 
  Ici, c'est un placeholder pour permettre au build de passer, le temps de restaurer l'arbo.</p>
</div>)}
